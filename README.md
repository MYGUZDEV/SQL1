# SQL1
Evaluación SQL entregable 8-11-21

En la creación de la tabla 'categorias' el id con primary key se lo realizó con varchar(20) para evitar las posibles consultas con INNER JOIN que aumentan la cantidad de procesos de consulta por row.
